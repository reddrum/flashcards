<div class="col-md-6 col-md-offset-3">
	<p>
		<%= image_tag @card.image.url(:medium) %>
	</p>
	<p>
		<h2>Original text</h1>
		<%= @card.original_text %>
	</p>
	<p>	
		<h2>Translated text</h1>
		<%= @card.translated_text %>
	</p>
	<p>
		<%= @card.review_date.to_datetime.strftime("%d/%m/%Y") %>
	</p>
	<%= link_to 'Edit', [:edit, @card.deck, @card] %> |
	<%= link_to 'Delete', deck_card_path(@card.deck, @card), method: :delete, data: { confirm: 'Are you sure?' } %>
</div>