<div class="col-md-6 col-md-offset-3">
	<% @cards.each do |card| %>
		<div class="template">
			<p><%= image_tag card.image.url(:medium) %></p>
			<h2><%= link_to card.original_text, card %></h2>
			<h4><%= card.review_date.to_datetime.strftime("%d/%m/%Y") %></h4>
		</div>	
	<% end %>
</div>